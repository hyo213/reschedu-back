# docker compose up 전에 이 파일을 .env로 복사하고 실제 값을 채워 넣는다.
RESCHEDU_DB_USER=reschedu
RESCHEDU_DB_PASS=changeme
JWT_SECRET=changeme
RESCHEDU_MAIL_USERNAME=
RESCHEDU_MAIL_PASSWORD=
APP_CORS_EXTRA_ALLOWED_ORIGINS=
# 학부모 대시보드 AI 리포트용 (https://aistudio.google.com/apikey 에서 발급). 비어있으면 AI 문장 없이 통계만 내려간다.
GEMINI_API_KEY=
